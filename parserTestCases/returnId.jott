#this should pass parsing but fail sematic analysis (phase3)
#return type does not match function return type and main must have a void return

Def main[]:Integer{
    Double x;
    x = 5.0;
	Return x;
}